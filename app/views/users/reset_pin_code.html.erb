<div class="flex justify-center items-center min-h-screen bg-gray-100">
  <div class="bg-white shadow-md rounded-lg overflow-hidden w-full max-w-md">
    <div class="p-6 bg-gray-50">
      <h1 class="text-2xl font-bold mb-4 text-center text-gray-800">PINコードの再設定</h1>
      <p class="text-sm text-gray-600 mb-4 text-center">登録済みのメールアドレスを入力し、新しいPINコードを設定してください。</p>

      <%= form_with url: update_pin_code_path, method: :post do |f| %>
        <div class="mb-4">
          <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
          <%= f.email_field :email, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
        </div>

        <div class="mb-4">
          <%= f.label :pin_code, "新しいPINコード", class: "block text-sm font-medium text-gray-700" %>
          <%= f.password_field :pin_code, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
        </div>

        <!-- 再設定ボタン -->
        <%= f.submit "再設定", class: "w-full py-2 px-4 mt-3 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      <% end %>
    </div>
  </div>
</div>
